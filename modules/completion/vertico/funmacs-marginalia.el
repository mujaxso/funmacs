;;; funmacs-marginalia.el --- Marginalia configuration -*- lexical-binding: t; -*-
;;; Commentary:
;; Adds annotations and icon support to Vertico minibuffer completion
;;; Code:

(use-package marginalia
  :ensure t
  :init
  (marginalia-mode))

(provide 'funmacs-marginalia)
;;; funmacs-marginalia.el ends here
