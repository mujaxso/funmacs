;;; funmacs-whichkey.el -*- lexical-binding: t; -*-

;;; Commentary:
;; which-key settings

;;; code

(use-package which-key
  :init (which-key-mode)
  :config (setq which-key-idle-delay 0.5))

(provide 'funmacs-whichkey)

;;; funmacs-whichkey.el ends here
